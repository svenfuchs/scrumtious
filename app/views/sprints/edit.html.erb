<p>
  <%= link_to 'Overview', projects_path %> »
  <%= h @sprint.name %>
</p>

<h1>Editing sprint</h1>

<% form_for(@sprint) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Update" %> or <%= link_to 'back', @sprint %>
  </p>
<% end %>